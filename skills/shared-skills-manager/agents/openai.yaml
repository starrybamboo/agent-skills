interface:
  display_name: "Shared Skills Manager"
  short_description: "Install, update, inspect, or schedule the shared skill catalog"
  default_prompt: "Use $shared-skills-manager to manage starrybamboo/agent-skills."
policy:
  allow_implicit_invocation: false
